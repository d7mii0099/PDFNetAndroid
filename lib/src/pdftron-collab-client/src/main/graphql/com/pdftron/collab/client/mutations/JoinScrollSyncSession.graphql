mutation JoinScrollSyncSession($sessionId: String!) {
        joinScrollSyncSession(sessionId: $sessionId) {
          leaderId
          documentId
          position {
            scrollTop
            scrollHeight
          }
          sessionId
        }
      }
