subscription OnScrollPositionChanged($sessionId: ID!) {
        scrollPositionChanged(sessionId: $sessionId) {
          position {
            scrollHeight
            scrollTop
          }
        }
      }
